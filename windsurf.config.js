/**
 * Конфигурация для деплоя в Windsurf
 */
module.exports = {
  /**
   * Настройки сервера
   */
  server: {
    // Порт, на котором будет работать приложение
    port: process.env.PORT || 5000,
    
    // Команда для запуска приложения
    start: 'npm run dev',
    
    // Путь к основному файлу приложения
    entry: 'server/index.ts',
  },
  
  /**
   * Настройки базы данных
   */
  database: {
    // URL-адрес для подключения к базе данных PostgreSQL
    // По умолчанию используется переменная окружения DATABASE_URL
    url: process.env.DATABASE_URL,
    
    // Тип базы данных
    type: 'postgresql',
  },
  
  /**
   * Настройки веб-сокетов
   */
  websockets: {
    // Включить поддержку веб-сокетов
    enabled: true,
  },
  
  /**
   * Настройки окружения
   */
  env: {
    // Переменные окружения, которые нужно установить
    required: ['DATABASE_URL'],
  },
};